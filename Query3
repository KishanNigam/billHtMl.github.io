both jobs are generating logs with the same name (APRRNG), and the job scan task always picks the latest log available. As a result, when the Multicheck job scans the latest log, it mistakenly selects the BiBerk log instead and triggers the email notification.
