Actually Desiree, Both jobs created Same name logs (APRRNG) and also when job scanning log its pick latest one that’s why when Multicheck job scanning the log its pick biberk log and through the mail.
